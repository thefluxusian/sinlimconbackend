<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="design">

	<select id="getUsers" parameterType="string" resultType="com.ctk.sinlimcon.vo.DesignVo">
		select
		*
		from design
		where userid=#{userId}
		order by lastupdatetime;
	</select>

	<select id="getUser" resultType="com.ctk.sinlimcon.vo.DesignVo">
		select
		*
		from design
		order by lastupdatetime;
	</select>
	
	<insert id="insertUser" parameterType="Design">
		insert into design values(#{designid},#{designname},#{userid},now(),now(),#{price},#{summary})
	</insert>
	
	<delete id="deleteUser" parameterType="string">
		delete from design where
		designid = #{designid}
	</delete>
	
	
	<update id="updateUser" parameterType="Design">
		update design 
		set
		designname = #{designname},
		userid = #{userid},
		lastupdatetime=now(),
		price=#{price},
		summary=#{summary}
		where designid = #{designid}
	</update>

</mapper>
